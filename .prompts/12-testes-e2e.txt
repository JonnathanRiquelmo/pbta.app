Implemente SOMENTE testes.

E2E (Playwright ou Cypress):
- Login Google (stub) → redirecionamento correto por `role`.
- Login email/senha (somente contas de teste):
  - `master.teste@pbta.dev` / `Test1234!` → redireciona para `/dashboard/master`.
  - `player.teste@pbta.dev` / `Test1234!` → redireciona para `/dashboard/player`.
  - Qualquer outro email/senha → erro "credenciais de teste inválidas".
- Mestre cria campanha, gera token; Jogador aceita; aparece em `players` como `accepted`.
- Jogador tenta criar ficha com soma != 3 → bloqueado.
- Rolagem vantagem (descarta menor) → `usedDice` corretos; outcome calculado.
- Mestre exclui rolagem; jogador não consegue excluir.

Unit (Vitest/RTL):
- Função de rolagem com modos e modifier.
- Validador de atributos (soma = 3).
