Implemente SOMENTE o fluxo de autenticação e detecção de papéis.

Requisitos:
- Tela `/login` com botão “Entrar com Google”.
- Ao autenticar, criar/atualizar `users/{uid}` com `email`, `displayName`, `role`, `createdAt`.
- Regra de papel: se `email === "jonnathan.riquelmo@gmail.com"` → `role: "master"`; caso contrário → `role: "player"`.
- Logout com limpeza de store e retorno para `/login`.
- Redirecionar: Mestre → `/dashboard/master`; Jogador → `/dashboard/player`.

 Login por email/senha (somente contas de teste):
 - Mestre: `master.teste@pbta.dev` com senha `Test1234!` → `role: "master"`
 - Jogador: `player.teste@pbta.dev` com senha `Test1234!` → `role: "player"`
 - Bloquear qualquer outro email/senha (exibir erro: "credenciais de teste inválidas").

Independência:
- Se não houver Firebase Auth, simule com stub local que persiste em memória.

Não altere outros módulos.
